import { EditCellDirective } from './edit-cell.directive';

describe('EditCellDirective', () => {
  it('should create an instance', () => {
    const directive = new EditCellDirective();
    expect(directive).toBeTruthy();
  });
});
